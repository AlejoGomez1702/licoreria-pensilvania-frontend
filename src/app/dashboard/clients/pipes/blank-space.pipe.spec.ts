import { BlankSpacePipe } from './blank-space.pipe';

describe('BlankSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new BlankSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
